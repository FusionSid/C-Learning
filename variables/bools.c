#include <stdio.h>
#include <stdbool.h> // You need to import this library to use bools in c
// Since there is no bools by default, Also bools arent even really needed cause you can just use 1 and 0
// or use #define to make them

int main()
{  
    /* Booleans: */

    // Creating a bool in c
    bool isSidCool = false;

    return 0;
}
